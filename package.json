{
  "name": "greenparty-forum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check admin.js && node --check data.js && node --check db.js && node --check migrate-mongo.js && node --check models/SiteState.js",
    "migrate:mongo": "node migrate-mongo.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mongoose": "^8.8.4"
  }
}
